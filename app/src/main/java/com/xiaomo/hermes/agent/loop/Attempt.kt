package com.xiaomo.hermes.agent.loop

/**
 * OpenClaw Source Reference:
 * - ../openclaw/src/agents/pi-embedded-runner/run/attempt.ts
 *
 * Single LLM call attempt within the agent loop.
 * Android: attempt logic is inlined in AgentLoop.runInternal() (Run.kt).
 */

object Attempt {
    // Attempt logic is part of the main loop in Run.kt.
    // OpenClaw separates this for testability; Android inlines it.
}
