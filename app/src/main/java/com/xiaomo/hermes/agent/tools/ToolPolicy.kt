package com.xiaomo.hermes.agent.tools

/**
 * OpenClaw Source Reference:
 * - ../openclaw/src/agents/pi-tools.policy.ts
 *
 * Per-tool policy evaluation (distinct from context/ToolPolicy which is prompt-level).
 */

object ToolPolicy {
    // Tool-level policy is currently evaluated inline in ToolCallDispatcher.
    // This stub exists for 1:1 file alignment.
}
