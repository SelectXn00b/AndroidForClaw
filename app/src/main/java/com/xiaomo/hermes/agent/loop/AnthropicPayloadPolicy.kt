package com.xiaomo.hermes.agent.loop

/**
 * OpenClaw Source Reference:
 * - ../openclaw/src/agents/anthropic-payload-policy.ts
 *
 * Anthropic payload policy (prompt caching, system prompt formatting).
 */

object AnthropicPayloadPolicy {
    // Payload formatting is handled in UnifiedLLMProvider/ApiAdapter.
}
