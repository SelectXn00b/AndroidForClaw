package com.xiaomo.androidforclaw.contextengine

fun ensureContextEnginesInitialized() {
    // Built-in context engine registration happens here.
    // The legacy context engine (direct message history) is always available.
}
