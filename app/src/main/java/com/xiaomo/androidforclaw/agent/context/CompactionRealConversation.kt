package com.xiaomo.androidforclaw.agent.context

/**
 * OpenClaw Source Reference:
 * - ../openclaw/src/agents/compaction-real-conversation.ts
 *
 * Real conversation compaction — summarizes long conversations while
 * preserving the most recent context.
 * See also: MessageCompactor.kt for the current implementation.
 */

object CompactionRealConversation {
    // Primary implementation is in MessageCompactor.kt.
    // This stub exists for 1:1 file alignment.
}
