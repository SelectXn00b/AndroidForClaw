package com.xiaomo.androidforclaw.agent.tools

/**
 * OpenClaw Source Reference:
 * - ../openclaw/src/agents/pi-tools.before-tool-call.ts
 *
 * Pre-tool-call validation and transformation.
 * See also: HookRunner.runBeforeToolCall() in agent/hook/
 */

object BeforeToolCall {
    // Before-tool-call logic is in HookRunner.runBeforeToolCall().
    // This stub exists for 1:1 file alignment.
}
